apiVersion: apiextensions.crossplane.io/v1
kind: CompositeResourceDefinition
metadata:
  name: xhelmreleases.vma.believe.com
spec:
  group: vma.believe.com
  names:
    kind: xHelmRelease
    plural: xhelmreleases
  versions:
    - name: v1alpha1
      served: true
      referenceable: true
      schema:
        openAPIV3Schema:
          type: object
          description: The Schema for the the helm release
          properties:
            spec:
              type: object
              description: Defines the desired state of a helm release
              properties:
                namespace:
                  type: string
                chartConfig:
                  type: object
                  description: chartConfig defines chart configuration
                  properties:
                    name:
                      type: string
                    repository:
                      type: string
                    version:
                      type: string
                resourceConfig:
                  type: object
                  description: ResourceConfig defines general properties of this resource.
                  properties:
                    name:
                      type: string
                    providerRef:
                      type: string
                      description: Which provider will be use to manage this resource
                    deletionPolicy:
                      type: string
                      description: Defaults to Delete
                      enum:
                        - Delete
                        - Orphan
                    tags:
                      type: object
                      description: Key-value map of resource tags.
                      additionalProperties:
                        type: string
                values:
                  type: string

