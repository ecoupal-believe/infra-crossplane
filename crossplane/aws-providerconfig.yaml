apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: aws-providerconfig
spec:
  dependsOn:
    - name: crossplane-aws-provider
  interval: 30s
  path: ../deploy/aws-providerconfig
  prune: true
  sourceRef:
    kind: GitRepository
    name: crossplane-sync
  timeout: 2m