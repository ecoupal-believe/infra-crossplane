apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: kafka-providerconfig
spec:
  dependsOn:
    - name: crossplane-kafka-provider
  interval: 30s
  path: ../deploy/kafka-providerconfig
  prune: true
  sourceRef:
    kind: GitRepository
    name: crossplane-sync
  timeout: 2m